# Rover skill catalog and bounds: the only source of rover skill bounds.
# robotd, brain, fakebox, and box/schema/*.json derive from this catalog.
# CI will fail on drift between the catalog and its derived contracts.
