# deploy — getting onto the Pi

An installer, three service units, and the device rule that gives the serial
link to one user and no one else.

Targets a clean Raspberry Pi OS 64-bit install. The camera stack comes from the
system packages, so the virtual environment is created with access to them.
