# Everything install.sh installs with --no-install-recommends (ARCHITECTURE 11
# step 7).  One package per line; # starts a comment.
#
# python3-picamera2 and python3-libcamera come from apt and never from pip: the
# venv is created --system-site-packages so rover-cam can see them.  They are
# apt-mark hold'ed by install.sh, together with libcamera0.7, because an
# upgrade that moves libcamera under a held picamera2 is the commonest way this
# camera stack breaks.

python3-picamera2      # the only camera API used (4.4)
python3-simplejpeg     # picamera2's still-encode path; named because of --no-install-recommends
git
make                   # README step 12's `make gates-pi`; not on a Lite image
libportaudio2          # what sounddevice needs and does not bundle
alsa-utils             # arecord -l, used by preflight
psmisc                 # fuser, which I-18 is tested with
ca-certificates
curl
